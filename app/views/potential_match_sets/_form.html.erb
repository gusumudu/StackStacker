<%= form_for(@potential_match_set) do |f| %>
  <% if @potential_match_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@potential_match_set.errors.count, "error") %> prohibited this potential_match_set from being saved:</h2>

      <ul>
      <% @potential_match_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
